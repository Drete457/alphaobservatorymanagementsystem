import {
  countryDelete,
  inputHandler,
  wasModifiedVerification,
} from './countries-handler';

const countriesHandler = {
  countryDelete,
  inputHandler,
  wasModifiedVerification,
};

export default countriesHandler;
