import {
  countryDelete,
  inputHandler,
  wasModifiedVerification,
} from './countries-handler';
import validateCountries from './validation';

const countriesHandler = {
  countryDelete,
  inputHandler,
  wasModifiedVerification,
  validateCountries,
};

export default countriesHandler;
